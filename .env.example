# BSC Configuration
# Copy this file to .env and configure for your environment

# Storage backend: "local" or "supabase"
BSC_STORAGE_BACKEND=local

# Local storage configuration directory
BSC_CONFIG_DIR=~/.config/bsc

# Session TTL in hours (default: 720 = 30 days)
BSC_SESSION_TTL_HOURS=720

# Supabase configuration (required when BSC_STORAGE_BACKEND=supabase)
# BSC_SUPABASE_URL=https://your-project.supabase.co
# BSC_SUPABASE_SERVICE_KEY=your-service-role-key

# Encryption key (auto-generated if not set)
# BSC_ENCRYPTION_KEY=your-base64-fernet-key
